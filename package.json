{
  "private": true,
  "scripts": {
    "clean": "rm -rf output bower_components node_modules site",
    "postinstall": "bower i --silent"
  },
  "devDependencies": {
    "pulp": "^12.0.1",
    "purescript": "^0.11.7"
  },
  "dependencies": {
    "npm-check-updates": "^2.14.0"
  }
}
