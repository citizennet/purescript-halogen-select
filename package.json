{
  "private": true,
  "scripts": {
    "preinstall": "npm run clean",
    "postinstall": "bower i --silent && pulp build",
    "clean": "rm -rf output bower_components node_modules",
    "build-docs": "pulp build -I site --to site/example.js",
    "watch-docs": "pulp -w build -I site --to site/example.js"
  },
  "devDependencies": {
    "pulp": "^12.0.1",
    "purescript": "^0.11.7"
  }
}
